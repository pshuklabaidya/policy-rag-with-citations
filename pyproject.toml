[project]
name = "ai-project-template"
version = "0.1.0"
description = "Reusable Streamlit-first AI portfolio project template."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
